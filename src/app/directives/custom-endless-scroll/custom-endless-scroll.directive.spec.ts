import { CustomEndlessScrollDirective } from './custom-endless-scroll.directive';

describe('CustomEndlessScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomEndlessScrollDirective();
    expect(directive).toBeTruthy();
  });
});
