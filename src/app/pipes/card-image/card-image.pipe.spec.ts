import { CardImagePipe } from './card-image.pipe';

describe('CardImagePipe', () => {
  it('create an instance', () => {
    const pipe = new CardImagePipe();
    expect(pipe).toBeTruthy();
  });
});
